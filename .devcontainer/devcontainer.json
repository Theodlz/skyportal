{
  "name": "skyportal",
  "build": {
    "dockerfile": "./Dockerfile"
  },
  "appPort": "5000:5000",
  "overrideCommand": false,
  "postCreateCommand": "cp docker.yaml config.yaml && source /skyportal_env/bin/activate && export NPM_CONFIG_LEGACY_PEER_DEPS=true && make system_setup"
}
